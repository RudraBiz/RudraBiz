# Supabase connection strings for project: RudraB (iizghxuzswteolishmrn)
#
# Get these from: Supabase Dashboard > Project Settings > Database > Connection String
# Replace [YOUR-PASSWORD] with your actual database password.

# Pooled connection (used by the app at runtime — via Supavisor on port 6543)
DATABASE_URL="postgresql://postgres.iizghxuzswteolishmrn:[YOUR-PASSWORD]@aws-0-ap-south-1.pooler.supabase.com:6543/postgres?pgbouncer=true"

# Direct connection (used only for running migrations — port 5432)
DIRECT_URL="postgresql://postgres.iizghxuzswteolishmrn:[YOUR-PASSWORD]@aws-0-ap-south-1.pooler.supabase.com:5432/postgres"

# Supabase API (optional, for future use — auth, storage, etc.)
NEXT_PUBLIC_SUPABASE_URL="https://iizghxuzswteolishmrn.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="get-this-from-supabase-dashboard-api-settings"

# NextAuth (Pass 2 — auth & company access)
# Generate with: npx auth secret
AUTH_SECRET="generate-a-real-secret-locally"
NEXTAUTH_URL="http://localhost:3000"
